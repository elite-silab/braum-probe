# Braum 布隆探针架构设计

> 文档版本：v3.0 · 更新日期：2026-07-27

## 1. 产品定位

Braum 是一个以 Cloudflare 单 Worker 为控制面、以轻量 Go Agent 为数据面的 VPS 监控平台。它监控真实 VPS 资源，并从每台 VPS 本地执行 HTTP/DNS 探测，不把 Cloudflare Cron 伪装成监控节点。

## 2. 高层架构

```mermaid
flowchart LR
    Browser["浏览器"] --> Worker["braum-probe Worker\nNext.js + Hono + Cron"]
    AgentA["VPS A · Braum Agent"] -->|"HTTPS 心跳 / 指标 / 探测结果"| Worker
    AgentB["VPS B · Braum Agent"] -->|"HTTPS 心跳 / 指标 / 探测结果"| Worker
    Cron["Cloudflare Cron Triggers"] --> Worker
    Worker --> D1["D1\n配置 / 指标 / 事件 / 审计"]
    Worker --> KV["KV\n缓存 / 限流"]
    Worker --> Assets["Workers Assets\nNext.js 静态资源"]
    Worker --> Notify["Telegram / Webhook"]
    AgentA --> Target["HTTP / DNS 目标"]
    AgentB --> Target
```

## 3. 单 Worker 请求分流

根入口 `worker.ts` 负责分流：

| 请求或事件 | 处理方 |
|---|---|
| `/health`、`/api/*` | Hono |
| 页面与 Next.js 资源请求 | OpenNext Worker |
| `scheduled` | Hono 调度器 |

网站与 API 同源，因此浏览器不需要生产 API 地址变量或跨域授权。D1、KV、Secrets、Cron 和 Assets 只绑定一次。

## 4. 组件职责

### Next.js 展示层

- App Router 提供公开状态页、节点详情、历史数据和管理后台。
- React Client Components 负责实时刷新、表单、主题与 JWT 会话。
- 公开详情页按请求读取 API，不把 D1 绑定暴露给浏览器。

### Hono 控制面

- Owner/Admin/Viewer 登录、鉴权与权限控制。
- 节点、目标、告警、公告、用户和审计管理。
- 创建一次性 Agent 注册令牌与安装命令。
- 校验 Agent 身份并接收心跳、指标和探测结果。
- 执行告警评估、小时/日聚合、心跳检查和数据清理。

### Braum Agent

- 以低权限 systemd 服务运行。
- 通过一次性令牌注册并保存节点独立密钥。
- 采集 CPU、内存、Swap、磁盘、负载、流量和主机信息。
- 获取目标配置，在 VPS 本地执行 HTTP/DNS 探测。
- 网络失败时指数退避，不要求 VPS 开放入站端口。

## 5. Agent 注册流程

```mermaid
sequenceDiagram
    participant A as 管理员
    participant W as braum-probe Worker
    participant V as VPS
    participant G as Braum Agent
    A->>W: 为节点生成安装命令
    W-->>A: 一次性令牌（15 分钟）
    A->>V: 执行安装命令
    V->>G: 安装并写入注册配置
    G->>W: POST /api/agent/v1/enroll
    W->>W: 原子消费令牌并生成节点密钥
    W-->>G: 仅返回一次 agent_secret
    G->>G: 以 0600 权限保存密钥
    G->>W: 使用节点密钥持续心跳
```

## 6. 节点状态

| 状态 | 判定 |
|---|---|
| 在线 | 最近心跳未超过 `max(3 × 上报周期, 180 秒)` |
| 离线 | 超过阈值且节点未暂停 |
| 暂停 | 管理员主动暂停 |
| 待注册 | 节点已创建但 Agent 尚未注册 |

## 7. 数据与可靠性

- D1 是配置、历史指标、探测结果、告警和审计的事实来源。
- KV 仅保存可重建的热点数据与限流状态。
- Agent 网络异常时指数退避；恢复后自动继续上报。
- Worker 失败不会让 Agent 开放端口或暴露长期密钥。
- 原始资源指标默认保留 7 天，探测结果 30 天，聚合和审计 90 天。

## 8. 远程终端边界

远程终端不属于普通心跳协议。后续实现必须使用短期会话票据、Agent 主动 WebSocket、管理员二次确认、会话吊销和完整审计，并默认关闭。

## 9. 非功能目标

| 类别 | 目标 |
|---|---|
| Agent 开销 | 空闲内存 < 30 MB；平均 CPU < 1% |
| 数据新鲜度 | 默认 60 秒并显示最后采集时间 |
| 安全 | TLS、摘要存储、一次性注册、RBAC、审计 |
| 兼容性 | Linux amd64/arm64 |
| 部署 | 一个 Worker、一份 Wrangler 配置、一次 Git 集成 |
